import { colors } from '../color';

export declare module '@mui/material/styles' {
  interface Theme {
    color: {
      gray: typeof colors.gray;
      smoke: typeof colors.smoke;
      jellyBean: typeof colors.jellyBean;
    }
  }
  interface ThemeOptions {
    color: {
      gray: typeof colors.gray;
      smoke: typeof colors.smoke;
      jellyBean: typeof colors.jellyBean;
    }
  }
}
